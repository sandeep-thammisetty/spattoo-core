{
  "//": "Copy-paste detector — a HARD GATE in `npm run verify` (and the pre-commit hook). `threshold` is the max % of duplicated lines allowed; the build fails above it. RATCHET IT DOWN as existing clones are paid off, so the gain can never silently erode. History: 2.82% (wiring) → 2.62% (heightfield→normalMap helper) → 2.21% (value-noise helper). Next clusters to extract: the mulberry32 PRNG, the FrostingPicker/FrostingStylePicker pair, and AgeNumber/CreamWriting cream-text. A deliberate, reviewed clone goes between `// jscpd:ignore-start` … `// jscpd:ignore-end` so it doesn't count.",
  "threshold": 2.3,
  "minTokens": 50,
  "minLines": 8,
  "absolute": true,
  "gitignore": true,
  "ignore": ["**/*.test.js", "**/*.test.jsx", "**/*.md", "**/dist/**", "**/.pw/**", "**/node_modules/**"],
  "reporters": ["console"]
}
